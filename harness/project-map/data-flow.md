# Data Flow

项目中数据的产生、存储、流转路径。

> 未检测到入口函数，无法自动推断数据流路径。
> 请使用 `--llm` 参数重新运行分析以启用 LLM 语义推断。
> 或在此手动填写数据流分析内容。


## 待分析的数据流 源码根: `app/analyzer`

- 入口函数 -> 处理节点 -> 数据变换路径
- （使用 --llm 自动生成，或手动填写）


## 变更规则

- 增加新的数据文件格式后必须更新本文档
- 增加新的数据流路径后必须更新上图
